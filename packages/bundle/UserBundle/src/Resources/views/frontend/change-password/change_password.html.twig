{% extends '@TalavUser/layout.html.twig' %}

{% trans_default_domain 'TalavUserBundle' %}

{% block talav_user_content %}

    {{ form_start(form, { 'action': path('talav_user_change_password') }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'talav.change_password.submit'|trans }}" />
    </div>
    {{ form_end(form) }}

{% endblock talav_user_content %}
